'''
inputs: data dir, dir of trained models

baseline on imgs:
    clustering of train img
    clustering of train seq
    gen:
    train img to test img
    train seq to test seq

generate representations:
    for each seq train model,
        seq rep
        frame rep
        still rep (which should be subset of above)
    for still trained model,
        still rep 

'''